[package]
name = "ak-backend-local"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
ak-core = { workspace = true }
tokio = { workspace = true }
async-trait = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }
tempfile = { workspace = true }
uuid = { workspace = true }
libc = { workspace = true }

[dev-dependencies]
axum = { workspace = true }

# The in-namespace egress forwarder for bwrap sandboxes (see the crate
# docs and `sandbox` module). std + libc only: it must run on any rootfs.
[[bin]]
name = "ak-egress-fwd"
path = "src/bin/ak_egress_fwd.rs"
